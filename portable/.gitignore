# Standalone Next.js build output (generated by build-portable.ps1)
app/server/

# App environment files (user-configured)
app/.env
app/.secrets

# Sensitive environment files (user-configured)
app/.env
app/.secrets

# Portable binary dependencies (downloaded by setup-dependencies.ps1)
node/
portable_caddy/
portable_db/
portable_minio/
portable_redis/
assets/

# Database and runtime data
db/

# Build intermediate cache
build-cache/
